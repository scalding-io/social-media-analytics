package io.scalding.approximations

import com.twitter.scalding._

/**
 * To calculate the size of CMS / HLL / BF for a particular sets of 10.000.000 unique elements,
 * we can get a good approximation by serializing into files.
 */
class Estimator(args:Args) extends Job(args){



}
